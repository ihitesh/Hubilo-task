export const appPrefix = "";
const appPaths = {
  root: `${appPrefix}/posts`,
  posts: `${appPrefix}/posts`,
  albums: `${appPrefix}/albums`,
  users: `${appPrefix}/users`,
  todos: `${appPrefix}/todos`,
};

export default appPaths;
